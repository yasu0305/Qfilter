{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Qt Debug (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/qfilter",
            "cwd": "${workspaceFolder}/build",
        }
    ]
}

